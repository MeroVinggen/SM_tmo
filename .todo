- does render till first action is made if connect after stream started, but does if connect before stream started
- does 720/1080 switcher works? 
  - add 480
- status is wrong - wrint connected but android app could even no be launched
  - should show "awaiting connection"
  - same when connection is lost and trying to re-connect

- update UI and app work to be closer to let view - main window and phone screan separate windows, phone in size of the phone, see what else
- hide notifications

- how to include web to exe to pack with the proj, or it will be portable if can't?

- publish to github




About:
- launch with: py -3.12 receiver.py
- build with go: go build -o screenmirror.exe .
  - or: go build ./...
